# Package

version       = "1.0"
author        = "Green Cardamom"
description   = "A library of awk functions in nim"
license       = "MIT"
srcDir        = "src"

# Dependencies

requires "nim >= 0.13.1"
requires "nre >= 1.0.0"

